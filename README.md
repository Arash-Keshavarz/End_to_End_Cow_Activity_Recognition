# End_to_End_Cow_Activity_Recognition

## Project Overview

Build a production-ready pipeline that detects cows in video, tracks them, classifies their activities, logs experiments, versions data, orchestrates workflows, deploys an inference API, and presents results in a dashboard.
